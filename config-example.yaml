postgres:
  host: "localhost"
  port: 5432
  user: "dev_user"
  password: "dev_password"
  db: "ask_app"

auth:
  secret_key: "secret"  # retrieve from `openssl rand -hex 32`
  algorithm: "HS256"
  access_token_expire_minutes: 30

log:
  level: "INFO"
